// Football API integration
export const fetchMatches = async () => {
    // Fetch matches from football API
    return [];
};

export const fetchMatchDetails = async (matchId: string) => {
    // Fetch details for specific match
    return null;
};
